<div id="btn" style="text-align: center ">

  <button
    onclick="location.href='/quiz';"
    class="button is-info is-outlined"
    style=""
  >start the quiz
  </button>

{{!-- {{#each topics as |topic|}} --}}
<div>
  {{!-- <h3><a href="/post/{{topic.id}}">{{topic.title}}</a></h3> --}}
  {{!-- <h3 id="post-home" ><a href="/post/{{topic.id}}" data-replace="get a link">{{topic.title}}</a></h3> --}}
  {{!-- <p>By: {{user.username}}</p>
</div>

   {{/each}}  --}}
{{!-- /bootstrap card --}}
{{#each topics as |topic|}}
 <div id="profile-card"  class="card" style="width: 18rem;">
  <div class="card-body">
    <h3 id="post-home" ><a href="/post/{{topic.id}}" data-replace="get a link">{{topic.title}}</a></h3>
    <p>By: {{user.username}}</p>
    </div>
    </div>

    {{/each}}
    {{!-- {{#each topics as |topic|}}
    <div class="container-fluid contenedor text-center">
  
   
   <div class=" container text-center">
      <div class="col-lg-4 col-md-4 col-sm-6 col-xs-12 container_foto ">
         <div class="ver_mas text-center">
            <span  class="lnr lnr-eye"></span>
         </div>
         <article class="text-left">
           <h3 id="post-home" ><a href="/post/{{topic.id}}" data-replace="get a link">{{topic.title}}</a></h3>
            <p>By: {{user.username}}</p>
         </article>
         
      </div>
      </div>
      {{/each}} --}}
    {{!-- {{#each topics as |topic|}}
    <div class="container">
  <div class="box">
    <span></span>
    <div class="content">
      <h3 id="post-home" ><a href="/post/{{topic.id}}" data-replace="get a link">{{topic.title}}</a></h3>
      <p>By: {{user.username}}</p>
      <a href="#">Read More</a>
    </div>
  </div>
  </div>
  {{/each}} --}}
      {{!-- <h6 class="card-subtitle mb-2 text-body-secondary">posted By:{{this.username}}</h6> 
{{!-- <p class="card-text">posted by:{{this.username}}</p> --}}
 {{!-- <p class="card-text">{{this.notes}}</p>
      <form id="comment-form">
        <textarea type="text" id="comment" placeholder="Text input"></textarea>
        <button>Submit</button>
</div>  --}}
